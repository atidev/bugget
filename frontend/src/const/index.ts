export enum BugStatuses {
  IN_PROGRESS = 0,
  READY = 1,
}

export enum ReportStatuses {
  IN_PROGRESS = 0,
  READY = 1,
}

export enum RequestStates {
  IDLE = 0,
  PENDING = 1,
  DONE = 2,
  ERROR = 3,
}
