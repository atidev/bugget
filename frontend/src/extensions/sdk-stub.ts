/**
 * Stub for @bugget/host-sdk in standalone builds
 */

export function initShared() {
  // Stub - never called in standalone
}

export function initExtensions() {
  // Stub - never called in standalone
  return Promise.resolve([]);
}
